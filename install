#!/usr/bin/env zsh
if [[ -z $STOW_FOLDERS ]]; then
    STOW_FOLDERS="emacs,tmux"
fi

if [[ -z $DOTFILES ]]; then
    DOTFILES=pwd
fi

STOW_FOLDERS=$STOW_FOLDERS

pushd $DOTFILES
for folder in $(echo $STOW_FOLDERS | sed "s/,/ /g")
do
    echo "stow $folder"
    stow -R $folder
done
popd
